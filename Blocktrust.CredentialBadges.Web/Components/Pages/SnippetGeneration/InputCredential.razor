@page "/credentialinput"

@rendermode InteractiveServer

<PageTitle>Input credential</PageTitle>

<div class="container text-center mt-5">
    <h1>Input Credential</h1>
    <input type="text" placeholder="Enter credential..." @bind="credentialInput" />
    <button class="btn btn-primary mt-3" @onclick="ContinueToPreview">Continue to Preview</button>
</div>

@code {
    [Inject] private NavigationManager NavigationManager { get; set; }

    private string credentialInput;

    private async Task ContinueToPreview()
    {
        // Simulate saving credential and getting an ID (replace with actual logic)
        Guid credentialId = Guid.NewGuid(); // Generate a new GUID for credential ID

        // Navigate to preview page with credential ID
        NavigationManager.NavigateTo($"/previewbadge/{credentialId}");
    }
}